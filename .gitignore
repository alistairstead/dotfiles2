/.agents/
/.claude/
node_modules/
# TTS mute flag (runtime state, toggled per machine)
claude/.claude/hooks/.speak-muted
# TTS binary, compiled from speak.swift by install.sh. ~/.claude/hooks is a
# stow symlink to this directory, so the build lands here.
claude/.claude/hooks/speak
karabiner/tsconfig.tsbuildinfo
